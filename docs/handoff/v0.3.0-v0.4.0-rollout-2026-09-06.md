# Handoff: v0.3.0 → v0.4.0 rollout (checkpoint-progress, dart-engineering)

Ngày ghi nhận: 2026-09-06

## Nguồn sự thật hiện tại

- Source chính: `F:\ai-agent\ai-engineering-skills`, branch `main`, đã push lên `origin/main`.
- Tip hiện tại trên remote: `fb491c5` (v0.4.0, 15 skill).
- Không có draft/staging nào bên ngoài — mọi thay đổi đi thẳng vào source chính rồi commit/push.

## Việc đã xong và đã verify

1. **v0.3.0 — skill `checkpoint-progress`** (workflow, thứ 14)
   - File: `skills/checkpoint-progress/SKILL.md`, `skills/checkpoint-progress/agents/openai.yaml`.
   - Dạy agent: đọc handoff note có sẵn trước khi resume việc dở; ghi note có cấu trúc tại `docs/handoff/<topic>-<date>.md` khi phải dừng giữa chừng, context sắp hết, hoặc chuyển agent khác.
   - Cập nhật kèm theo: `skillset.json` (version 0.3.0, thêm entry), `.claude-plugin/plugin.json` + `.codex-plugin/plugin.json` (version), `README.md` (danh sách skill), `CHANGELOG.md`.
   - Sửa `scripts/test-tooling.ps1` để tự đọc số skill/version từ `skillset.json` (biến `$expectedSkillCount`, `$manifest.version`) thay vì hardcode — tránh vỡ test mỗi lần thêm skill.
   - Commit: `020ea7c` (không có trailer Co-Authored-By, theo yêu cầu của repo owner — xem mục "Quyết định quan trọng").
   - Verify: `validate-skills.ps1` → 14 skill pass; `test-tooling.ps1` → 13/13 pass; `git diff --check` sạch.

2. **v0.4.0 — skill `dart-engineering`** (stack, thứ 15)
   - File: `skills/dart-engineering/SKILL.md`, `skills/dart-engineering/agents/openai.yaml`.
   - Theo đúng khuôn Inspect First / Engineering Guidance / Verification của 5 stack skill có sẵn (dotnet, java, go, python, typescript).
   - Nội dung: null-safety soundness, dispose controller/stream/timer trong `State`, không dùng `BuildContext` qua `await` mà không check `mounted`, không sửa tay generated file (`*.g.dart`, `*.freezed.dart`, `*.gr.dart`), giữ nguyên state-management pattern hiện có, coi platform-channel contract là một phần bề mặt ảnh hưởng.
   - Cập nhật kèm theo: `skillset.json` (version 0.4.0), 2 plugin manifest, `README.md`, `CHANGELOG.md`.
   - Commit: `fb491c5` (không trailer).
   - Verify: `validate-skills.ps1` → 15 skill pass; `test-tooling.ps1` → 13/13 pass (tự cập nhật số lượng nhờ fix ở bước trên); `git diff --check` sạch.

3. **Cả 2 commit đã push lên `origin/main`** (`020ea7c`, `fb491c5`).

4. **Áp dụng vào dự án tiêu thụ `F:\snap-video`** (Flutter app):
   - Cài lần đầu ở v0.3.0: `install-skills.ps1 -Target Universal -Scope Project -ProjectRoot F:\snap-video` (không `-Overwrite`, vì chưa có gì trước đó).
   - Cập nhật lên v0.4.0: `install-skills.ps1 -Target Universal -Scope Project -ProjectRoot F:\snap-video -Overwrite`.
   - Đích cài: `F:\snap-video\.agents\skills\` (15 skill canonical) và `F:\snap-video\.claude\skills\ai-engineering-skills\` (nested plugin cho Claude Code).
   - Receipt xác nhận: `F:\snap-video\.agents\skills\.ai-engineering-skills.receipt.json` — `version: 0.4.0`, đủ 15 skill kể cả `dart-engineering`.

## Quyết định quan trọng và lý do

- **License MIT** — chủ repo (Van Nguyen) chọn ngày 2026-09-06.
- **Không thêm dòng `Co-Authored-By`/`Claude-Session` vào commit message của repo này** — chủ repo yêu cầu xoá khỏi 2 commit đầu (đã rewrite history + force-push ngày 2026-09-06) và giữ quy ước này cho mọi commit sau đó.
- **`test-tooling.ps1` không còn hardcode số skill/version** — lý do: mỗi lần thêm skill mới (như `checkpoint-progress`, `dart-engineering`) trước đó phải sửa tay nhiều dòng test, dễ sót và gây fail giả. Giờ test tự đọc từ `skillset.json`.
- **`dart-engineering` được thêm theo yêu cầu cụ thể** vì dự án tiêu thụ đầu tiên (`snap-video`) là Flutter/Dart và bộ skill trước đó chưa có stack skill cho ngôn ngữ này.

## Lệnh verify cần chạy lại nếu tiếp tục

```powershell
cd F:\ai-agent\ai-engineering-skills
.\scripts\validate-skills.ps1
.\scripts\test-tooling.ps1
git diff --check
```

Kỳ vọng: 15 skill, version 0.4.0, 13/13 test pass, diff --check sạch.

## Việc còn thiếu / có thể làm tiếp

- Chưa có skill/stack nào khác ngoài Dart được thêm theo yêu cầu dự án cụ thể — nếu áp dụng bộ skill vào dự án dùng ngôn ngữ khác (Rust, PHP, Kotlin thuần, v.v.) sẽ gặp khoảng trống tương tự.
- README/CHANGELOG/CONTRIBUTING đã cập nhật đầy đủ tới v0.4.0; không có việc dở dang nào khác được biết tại thời điểm ghi note này.
- Chưa kiểm tra `dart-engineering` skill bằng cách thực sự dùng nó trong một task thật trên `snap-video` — nội dung mới chỉ được review qua đọc lại, chưa có phản hồi thực tế từ việc dùng.

## Bước tiếp theo đề xuất

1. Dùng thử `snap-video` với các skill vừa cài (đặc biệt `dart-engineering` và `checkpoint-progress`) trong một task thật, thu thập phản hồi.
2. Nếu phát hiện guidance nào trong `dart-engineering` chưa sát thực tế dự án Flutter (ví dụ convention riêng của `snap-video`), sửa tại `F:\ai-agent\ai-engineering-skills`, bump patch/minor version, rồi cài lại bằng `-Overwrite`.
3. Cân nhắc thêm stack skill khác nếu có dự án tiêu thụ mới dùng ngôn ngữ chưa được hỗ trợ.
